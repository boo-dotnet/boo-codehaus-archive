# BOO-432: [IDE Integration] Arbitrary retrieval of AST nodes

| Field | Value |
|---|---|
| Type | New Feature |
| Status | Open |
| Priority | Minor |
| Resolution | Unresolved |
| Reporter | Arron Washington |
| Assignee | Unassigned |
| Affects | 0.8 |
| Fix for | 0.9.5 |
| Components | Boo.Lang.CodeDom, Boo.Lang.Useful |
| Created | 2005-08-09T15:16-0500 |
| Updated | 2011-01-21T09:32-0600 |

## Description

Right now if I want to grab the AST node located at a lexical position, I have to manually dive into the target module and drill down, keeping a copy of the "last valid" ast node until I finally hit an AST node whose lexical info is greater than the last valid node and greater than the target lexical info. Fuzzy resolution.

It is a suprising amount of work and prone to errors!

It would be nice to be able to arbitrarily retrieve AST nodes by type or lexical information.

Here is some poorly thought-out sample code:

#'context' is a CompilerContext

context = boo.Run()

code = CodeInformationService(context.CompileUnit) #accepts any valid 'node'

node = code.GetNodeAt(line, column, filename)

#alternatively, if you've passed a Module instead of a CompileUnit ...

node = code.GetNodeAt(line, column)

/* do dirty, dirty things to this node. */

Or...

#This is useful when creating class diagramming software – we might only care about certain types or members.

nodes = code.GetNodes(ClassDefinition) #we passed a CompileUnit on init, so we'll get all of the ClassDefinitions

code.SetRoot(context.Module[3]) #we're only interested in inspecting the fourth module

nodes2 = code.GetNodes(ClassDefinition) #only get ClassDefinitions from the fourth module

/* generate UML diagram here */

I've made up the syntax on the spot here, but hopefully you know what I be talkin' 'bout.

## Provenance

- <http://web.archive.org/web/20150507195752id_/https://jira.codehaus.org/browse/BOO-432>
- <http://web.archive.org/web/20150511143219id_/https://jira.codehaus.org/browse/BOO-432?page=com.atlassian.jira.plugin.system.issuetabpanels:comment-tabpanel>
