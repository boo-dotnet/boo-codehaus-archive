# BOO-988: optional 'else' block for 'for' and 'while' loops

| Field | Value |
|---|---|
| Type | New Feature |
| Status | Resolved |
| Priority | Minor |
| Resolution | Fixed |
| Reporter | Cedric Vivier |
| Assignee | Marcus Griep |
| Affects | 0.8.1 |
| Fix for | 0.8.2 |
| Components | None |
| Created | 2008-04-08T04:08-0500 |
| Updated | 2008-04-12T08:06-0500 |
| Resolved | 2008-04-08T10:51-0500 |

## Description

```
for &lt;target_list&gt; in &lt;enumerable&gt;:
 &lt;for_block&gt;
else:
 &lt;else_block&gt;

```

&lt;else_block&gt; is only executed when the loop terminates because &lt;enumerable&gt;.MoveNext() returns false when evaluated, i.e. whenever the &lt;enumerable&gt; attempts to move to the next item, but the &lt;enumerable&gt; is exhausted. This also means that if &lt;enumerable&gt; has no item at the first 'iteration', &lt;enumerable&gt;.MoveNext() will return false, and the &lt;else_block&gt; will be executed.

```
while &lt;expression&gt;:
 &lt;while_block&gt;
else:
 &lt;else_block&gt;

```

&lt;else_block&gt; is only executed when the loop terminates the &lt;expression&gt; is tested but resolves to a false, null, or 0 value. This also means that if &lt;expression&gt; is false the first time it is tested (even before entering &lt;while_block&gt;) &lt;else_block&gt; will be executed.

The effect is that for these loops, &lt;else_block&gt; will not be executed when execution leaves the loop due to a break, raise, or return because &lt;expression&gt;/&lt;enumerable&gt;.MoveNext() is not evaluated. Note that the cause of the &lt;else-block&gt; executing is the condition evaluating to false.

## Comments (2)

### comment-130128

Hide

 Permalink

 Marcus Griep
 added a comment - 08/Apr/08 8:57 AM 

This change should be minimally invasive, and as it adds to the language without adversely increasing complexity, I have no problems with it being implemented. 

 Show

 Marcus Griep
 added a comment - 08/Apr/08 8:57 AM This change should be minimally invasive, and as it adds to the language without adversely increasing complexity, I have no problems with it being implemented.

### comment-130159

Hide

 Permalink

 Marcus Griep
 added a comment - 08/Apr/08 1:15 PM - edited 

The current flow is equivalent to:

```

:looptarget
if condition:
 loop_block
 goto looptarget
else:
 else_block
:afterloop

```

There is discussion going on to change the flow to be equivalent to:

```

condMet = false
:looptarget
if condition:
 condMet = true
 loop_block
 goto looptarget
elif condMet:
 else_block
:afterloop

```

where a break in loop_block would evaluate to:

```

goto afterloop

```

 Show

 Marcus Griep
 added a comment - 08/Apr/08 1:15 PM - edited The current flow is equivalent to: 

:looptarget
 if condition:
 loop_block
 goto looptarget
 else :
 else_block
:afterloop

 There is discussion going on to change the flow to be equivalent to: 

condMet = false 
:looptarget
 if condition:
 condMet = true 
 loop_block
 goto looptarget
elif condMet:
 else_block
:afterloop

 where a break in loop_block would evaluate to: 

 goto afterloop

## Provenance

- <http://web.archive.org/web/20150515152500id_/http://jira.codehaus.org/browse/BOO-988>
- <http://web.archive.org/web/20150517100816id_/https://jira.codehaus.org/browse/BOO-988?page=com.atlassian.jira.plugin.system.issuetabpanels:comment-tabpanel>
