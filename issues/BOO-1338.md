# BOO-1338: IQuackFu cache does not distinguish properly between set/get/invoke

| Field | Value |
|---|---|
| Type | Bug |
| Status | Open |
| Priority | Major |
| Resolution | Unresolved |
| Reporter | Tristan Zajonc |
| Assignee | Unassigned |
| Affects | 0.9.5 |
| Fix for | None |
| Components | Runtime (Boo.Lang) |
| Created | 2011-08-28T13:24-0500 |
| Updated | 2011-08-28T16:24-0500 |

## Description

The cache in Boo.Lang.Runtime.RuntimeServices._cache contains only name of the method, and types of arguments. It doesn't distinguish between property and method calls. This causes bugs when implementing IQuackFu.

The possible fix is to change Boo.Lang.Runtime.RuntimeServices.SetProperty

from:

 public static object SetProperty(object target, string name, object value)

 {

 object[] args = new[] 
{ value };

 var dispatcher = GetDispatcher(target, args, name, () =&gt; CreatePropSetDispatcher(target, name, value));

 return dispatcher(target, args);

 }

to:

 public static object SetProperty(object target, string name, object value)

 {

 var setterName = "set_" + name;

 object[] args = new[] { value }

;

 var dispatcher = GetDispatcher(target, args, setterName, () =&gt; CreatePropSetDispatcher(target, setterName, value));

 return dispatcher(target, args);

 }

The same need to be done for GetProperty.

Hattip: Maksym Trushyn.

## Comments (1)

---

### Maksym Trushyn, 28/Aug/11 4:24 PM

Sorry I suggest the fix which is not backward compatible. Possibly, the better fix is:

public static object SetProperty(object target, string name, object value)

{

 object[] args = new[] 
{ value }

;

 var dispatcher = GetDispatcher(target, args, "set_" + name, () =&gt; CreatePropSetDispatcher(target, name, value));

 return dispatcher(target, args);

}

## Provenance

- <http://web.archive.org/web/20150517103855id_/https://jira.codehaus.org/browse/BOO-1338>
- <http://web.archive.org/web/20150520081344id_/https://jira.codehaus.org/browse/BOO-1338?page=com.atlassian.jira.plugin.system.issuetabpanels:comment-tabpanel>
