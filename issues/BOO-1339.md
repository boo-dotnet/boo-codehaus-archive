# BOO-1339: Generic constraint not correctly applied when type is declared on a field

| Field | Value |
|---|---|
| Type | Bug |
| Status | Open |
| Priority | Major |
| Resolution | Unresolved |
| Reporter | Giovanni Bassi |
| Assignee | Unassigned |
| Affects | 0.9.4 |
| Fix for | None |
| Components | Compiler |
| Created | 2011-09-06T14:03-0500 |
| Updated | 2011-09-06T14:03-0500 |

## Description

When you declare a field's type explicitly, if it's type is a generic class with a constraint, then under certain conditions the type will no longer have the generic constraint, only the generic constraint remains.

This will not compile as expected, class Repository will be defined as Repository[of T] instead of Repository[of T(Entity)]:

class Other:

 rep as Repository of Entity = Repository of Entity()

class Repository[of T(Entity)]:

 pass

class Entity:

 pass

But if you leave the class Other at the bottom, it works as expected:

class Repository[of T(Entity)]:

 pass

class Entity:

 pass

class Other:

 rep as Repository of Entity = Repository of Entity()

Also, if you remove the type definition, you no longer see the problem:

class Other:

 rep = Repository of Entity()

class Repository[of T(Entity)]:

 pass

class Entity:

 pass

## Provenance

- <http://web.archive.org/web/20150520081124id_/https://jira.codehaus.org/browse/BOO-1339>
- <http://web.archive.org/web/20150521151744id_/https://jira.codehaus.org/browse/BOO-1339?page=com.atlassian.jira.plugin.system.issuetabpanels:comment-tabpanel>
