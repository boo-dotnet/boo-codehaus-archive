# BOO-313: changing struct values accessed with properties

| Field | Value |
|---|---|
| Type | Bug |
| Status | Reopened |
| Priority | Major |
| Resolution | Unresolved |
| Reporter | Joachim Ante |
| Assignee | Rodrigo B. de Oliveira |
| Affects | 0.5, 0.9.1 |
| Fix for | 0.5.6, 0.9.5 |
| Components | None |
| Created | 2005-05-01T15:14-0500 |
| Updated | 2011-01-21T09:32-0600 |

## Description

I have a struct Vector3 in created with C# in a dll which is used by a boo script:

struct Vector3 
{ float x,y, z; }

And a class Transform:

class Transform

{

 public Vector3 position { get 
{ ... }

 set { ... }}

}

When i write in boo:

Transform transform = Transform ()

transform.position.x += 5

Nothing happens. I presume it will just change the value of the struct and then throw the struct away.

Boo should instead transform the code to something like this: 

temp = transform.position;

temp.x += 5

transform.position = temp

## Comments (3)

### comment-183305

Hide

 Permalink

 Cedric Vivier
 added a comment - 12/Jul/09 3:08 AM 

Reopening.

To make it work with nested properties and indexed properties as well, we should probably just use the address of the struct.

 Show

 Cedric Vivier
 added a comment - 12/Jul/09 3:08 AM Reopening. 

 To make it work with nested properties and indexed properties as well, we should probably just use the address of the struct.

### comment-183247

Hide

 Permalink

 Dmitry Malyshev
 added a comment - 10/Jul/09 2:54 PM 

We need to have a support for indexed properties as well.

```

class B:
 struct C:
 a as int
 q = (C(a:1),C(a:2))
 public Q[i as uint] as C:
 get: return q[i]
 set: q[i] = value
 b = B()
 b.Q[0].a = -1

```

produces warning:

Assignment to temporary. (BCW0006) - C:\pr\boo\demo\demo.boo:85,12

the b.q[0].a value is not changed in the result 

 Show

 Dmitry Malyshev
 added a comment - 10/Jul/09 2:54 PM We need to have a support for indexed properties as well. 

class B:
 struct C:
 a as int 
 q = (C(a:1),C(a:2))
 public Q[i as uint] as C:
 get: return q[i]
 set: q[i] = value
 b = B()
 b.Q[0].a = -1

 produces warning: 
Assignment to temporary. (BCW0006) - C:\pr\boo\demo\demo.boo:85,12 
the b.q [0] .a value is not changed in the result

### comment-39229

Hide

 Permalink

 Rodrigo B. de Oliveira
 added a comment - 17/May/05 6:08 PM 

I've just checked in a fix to the simple case above. But the same problem can still happen with nested property accesses:

struct Point:

 public x as int

 public y as int

struct Rectangle:

 [property(topLeft)]

 _top as Point

class Figure:

 [property(rect)]

 _rect as Rectangle

f = Figure()

f.rect.topLeft.x = 10

assert 10 == f.rect.topLeft.x 

 Show

 Rodrigo B. de Oliveira
 added a comment - 17/May/05 6:08 PM I've just checked in a fix to the simple case above. But the same problem can still happen with nested property accesses: 

 struct Point: 
 public x as int 
 public y as int 

 struct Rectangle: 
 [property(topLeft)] 
 _top as Point 

 class Figure: 
 [property(rect)] 
 _rect as Rectangle 

 f = Figure() 
f.rect.topLeft.x = 10 

 assert 10 == f.rect.topLeft.x

## Provenance

- <http://web.archive.org/web/20150507195741id_/https://jira.codehaus.org/browse/BOO-313>
- <http://web.archive.org/web/20150517103608id_/https://jira.codehaus.org/browse/BOO-313?focusedCommentId=183247&page=com.atlassian.jira.plugin.system.issuetabpanels:comment-tabpanel>
